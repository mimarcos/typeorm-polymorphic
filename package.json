{
  "name": "typeorm-polymorphic",
  "version": "1.0.0",
  "homepage": "https://github.com/bashleigh/typeorm-polymorphic",
  "dependencies": {
    "@types/node": "^14.14.14",
    "typeorm": "^0.2.28"
  },
  "scripts": {
    "clean": "rm -R dist/",
    "build": "npm run clean && tsc -p src"
  }
}
